#include <stdio.h>

int main()
{
    FILE *ptr = NULL;
    // ptr = fopen("myfile.txt", "r"); // here only read occured
    // char c = fgetc(ptr);
    // printf("The character I read was %c\n", c);
    // c = fgetc(ptr);
    // printf("The character I read was %c\n", c);
    // fclose(ptr);
    // char str[5];
    // fgets(str, 5, ptr);
    // printf("The string is %s\n", str);
    // fgets(str, 5, ptr);
    // printf("The string is %s\n", str);
    // fclose(ptr);
    // ptr = fopen("myfile.txt", "w"); // here full clear occured
    // fputc('o', ptr);
    // fputs("My name is Soham Chakrabortty.", ptr);
    // fputs("My name is Soham Chakrabortty.\nk", ptr);
    // fclose(ptr);
    // ptr = fopen("myfile.txt", "r+"); // here replace occured
    // fputc('o', ptr);
    // fputs("My name is Soham Chakrabortty.", ptr);
    // fclose(ptr);
    // ptr = fopen("myfile.txt", "w+"); // here full clear occured
    // fputc('o', ptr);
    // fputs("My name is Soham Chakrabortty.", ptr);
    // fclose(ptr);
    // ptr = fopen("myfile.txt", "a"); // here apend occured
    // fputc('o', ptr);
    // fputs("My name is Soham Chakrabortty.", ptr);
    // fclose(ptr);

    return 0;
}
